# The Android build (docs/android-port-plan.md).
#
# ONE JOB, `playable`, ON DEMAND ONLY. There used to be three: `apk` built a stub-image APK, `boot`
# installed it on an x86_64 emulator and ran --smoke, and `playable` built the real thing from a URL
# given at dispatch time. The first two are gone, and what that costs is written down here rather
# than left to be rediscovered by whoever next wonders why nothing caught a break:
#
#   * NOTHING IN THIS FILE RUNS ON A PUSH OR A PULL REQUEST ANY MORE. build.yml still runs on both,
#     and still compile-checks the four Android sources that are inert everywhere else, because they
#     are in the runtime's shared source list — but it builds for the HOST, so what is no longer
#     exercised anywhere automatically is the arm64 cross-compile, the four cross-built dependencies
#     and the packaging. Those happen only when somebody dispatches this.
#   * THE EMULATOR GATE IS GONE, and it was the only thing in this repository that proved anything
#     about RUNNING rather than compiling: an install, a start of GameActivity with
#     `--esa cwArguments --smoke`, and logcat carrying the harness's own verdict. Nothing replaces
#     it. A green run here means an APK was produced. It does not mean the APK boots.
#
# WHY ONLY workflow_dispatch. The job needs a URL to a copyrighted executable, and on a push or a
# pull request there is nobody to supply one and no mechanism to supply it with — the job could only
# skip. A trigger whose every run is skipped is noise that reads like coverage.
name: android

on:
  workflow_dispatch:
    inputs:
      # Every input belongs to the one job. `run_emulator_gate` went with the job it gated: an input
      # that cannot change anything is a control that lies about being one.
      abi:
        description: "ABI to build"
        type: choice
        default: arm64-v8a
        options: [arm64-v8a, x86_64]
      xex_url:
        description: "DIRECT download URL. Give the STFS PACKAGE, not a bare default.xex: the package also carries data/shaders/*.big, which is the only source the shader cache can be built from. Leave empty to use the CW_XEX_URL secret. On a PUBLIC repo this field is visible on the run page."
        type: string
        default: ""
      xex_sha256:
        description: "Optional SHA-256 of that file, to prove the download is the image you meant"
        type: string
        default: ""
      artifact:
        description: "What to publish. This repo is PUBLIC: an APK built from your XEX contains game-derived code, and Actions artifacts on a public repo can be downloaded by anyone."
        type: choice
        default: "logs only"
        options:
          - "logs only"
          - "APK too - publishes game-derived data publicly for 1 day"

env:
  # The same pinned bases build.yml uses, and for the same reason: the vendored patches in
  # tools/ci/ apply onto these commits, and a stale pin fails loudly at `git apply` naming the
  # script that regenerates them.
  XENON_BASE: ddd128bcca99fe8bfbb99bea583c972351fa6ace
  XENOS_BASE: 990d03b28a27b50277ee5d8d942e1c5f873869d1
  # Pinned here rather than discovered, because "the newest Gradle" is a build variable nobody
  # decided on. Must match android/gradle/wrapper/gradle-wrapper.properties.
  GRADLE_VERSION: "8.11.1"
  # Where the recompilers are, for tools/android/build_apk.sh and everything it calls. Its default
  # is the developer convention ($HOME/GithubRepo/<name>, the same one runtime/CMakeLists.txt uses)
  # and CI clones them beside the checkout instead, so without these the failure is "no XenonRecomp
  # checkout at /home/runner/GithubRepo/XenonRecomp" for a checkout that exists and was patched two
  # steps earlier. build_apk.sh also falls back to the sibling and says so; being explicit here is
  # better than relying on a fallback, and the fallback is for a human in a CI-shaped checkout.
  CW_XENON_ROOT: ${{ github.workspace }}/../XenonRecomp
  CW_XENOS_ROOT: ${{ github.workspace }}/../XenosRecomp

jobs:
  # -----------------------------------------------------------------------------------------
  # THE PLAYABLE BUILD — the only job in this repository that puts the real guest image in an
  # artifact, and therefore the only one that needs a copy of the game.
  #
  # It is opt-in by necessity rather than by preference: no runner may hold copyrighted game data as
  # a matter of course, so this is one person's own dispatch, with the URL supplied by that person at
  # that moment — the difference between a repository that distributes a game and a repository whose
  # owner built their own copy on rented hardware. The two stub-image jobs that used to sit beside
  # it, proving compile / link / package / boot against tools/gen_stub_ppc.py, are gone; what they
  # proved about COMPILING is build.yml's job now, and what they proved about BOOTING nothing
  # proves. See the head of this file for what that costs.
  #
  # WHAT IT DOES: download default.xex (or the STFS package, which it unpacks), generate ppc/
  # with the PATCHED host XenonRecomp, verify the generation actually happened, cross-compile the
  # four dependencies, and build an APK whose guest image is the title's own 58k functions.
  #
  # SHADERS ARE TWO STEPS HERE, AND THEY ARE THE DIFFERENCE BETWEEN AN APK THAT BOOTS AND ONE THAT
  # DRAWS. gpu/shader_translator.cpp dlopens libdxcompiler.so and nothing else in the pipeline
  # compiles anything: microcode -> HLSL is XenosRecomp's recompiler inside the runtime, HLSL ->
  # SPIR-V is DXC. So this job
  #
  #   * builds the CACHE on the runner from the game's own banks, with the x86_64 DXC XenosRecomp
  #     already vendors — SPIR-V is architecture-independent and the cache key is the hash of the
  #     MICROCODE, so what an x86_64 runner writes is what an arm64 phone looks up;
  #   * cross-compiles DXC ITSELF for arm64 (--with-dxc: an LLVM build, tools/android/build_dxc.sh)
  #     because no prebuilt one exists — Microsoft ships Linux x86_64 only, in every release
  #     including v1.9.2607 — and packages it as a jniLib, which lands in nativeLibraryDir, which
  #     IS HostPaths::ExeDir() on Android, which gpu/shader_translator.cpp already searches.
  #
  # The cache this produces is close to complete, because tools/release/vs_recipes.bin IS tracked
  # here: pixel shaders verbatim from the disc bank, plus 102 of 104 vertex shaders as
  # template-plus-patch. The two that remain are engine-synthesised with no template anywhere and
  # are bound before the first visible frame, which is what makes the arm64 DXC above a requirement
  # rather than an optimisation.
  #
  # Give this job the STFS PACKAGE, not a bare default.xex: the banks are in data/, and the
  # executable does not contain them. With a bare XEX the job still produces a correct ppc/ and a
  # correct DXC, and every shader is then translated on the phone at first sight instead.
  # -----------------------------------------------------------------------------------------
  playable:
    # No `if` here. workflow_dispatch is the only trigger this file has, so a condition testing for
    # it could not be false, and a guard that cannot fire is one a reader has to reason about to
    # discover it does nothing.
    runs-on: ubuntu-latest
    # 360 is GitHub's CEILING for a job on a hosted runner, not a guess, and this job can reach it:
    # generating 58k functions, compiling 228 arm64 translation units of them, translating ~1,265
    # pixel shaders and cross-compiling LLVM's DXC are all in the same run on four cores. What makes
    # that tolerable is that every expensive step except the guest image is cached — the four
    # dependencies, the DXC prefix, and the Gradle caches — so a re-run pays for generation, the
    # image and the shader pass only. ppc/ is deliberately NOT cached: it is game-derived, and this
    # job's whole posture is that such data is present for one dispatch by one person rather than
    # stored somewhere it persists.
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4

      - name: resolve the download URL
        env:
          INPUT_URL: ${{ inputs.xex_url }}
          SECRET_URL: ${{ secrets.CW_XEX_URL }}
        run: |
          # The secret is the channel to prefer, and not only for tidiness: on a PUBLIC repo a
          # workflow_dispatch input is recorded on the run page, which is public, so a link typed
          # into the form is a link anybody watching this repository can read. add-mask keeps the
          # value out of the LOG, which is the part this job controls; it cannot retract what the
          # dispatch form already published. That is why the secret exists.
          URL="${INPUT_URL:-$SECRET_URL}"
          if [ -z "$URL" ]; then
            echo "::error::No URL. Set the CW_XEX_URL repository secret (preferred) or fill the xex_url input."
            exit 1
          fi
          if [ -n "$INPUT_URL" ]; then
            echo "::warning::xex_url was passed as a dispatch input on a PUBLIC repository, so the value is visible on this run's page. Delete and re-run with the CW_XEX_URL secret if that link is not already public."
          fi
          echo "::add-mask::$URL"
          echo "XEX_URL=$URL" >> "$GITHUB_ENV"
          echo "url resolved and masked (it will not be printed again by any step below)"

      - name: download it
        run: |
          curl -fL --retry 3 --retry-delay 5 --connect-timeout 30 -o /tmp/download.bin "$XEX_URL"
          echo "downloaded $(stat -c%s /tmp/download.bin) bytes"
          if [ -n "${{ inputs.xex_sha256 }}" ]; then
            echo "${{ inputs.xex_sha256 }}  /tmp/download.bin" | sha256sum -c - \
              || { echo "::error::SHA-256 mismatch: the download is not the file you meant."; exit 1; }
          fi

      - name: work out what was downloaded
        run: |
          python3 - <<'PY'
          import os, sys, subprocess
          magic = open('/tmp/download.bin','rb').read(4)
          print(f"first four bytes: {magic!r}")
          if magic == b'XEX2':
              kind = 'xex'
          elif magic in (b'CON\x00', b'LIVE', b'PIRS'):
              kind = 'package'
          elif magic[:1] in (b'<', b'{'):
              # The failure worth naming specifically: a share page rather than a file. curl -fL
              # follows redirects and happily saves the HTML a file host serves for a link that
              # was never a direct-download link, and the error would otherwise arrive forty
              # minutes later as "XenonRecomp produced nothing".
              print("::error::That URL served a web page, not a file. Google Drive/Dropbox/OneDrive "
                    "share links do this. You need a DIRECT download link (for Drive, "
                    "https://drive.google.com/uc?export=download&id=<ID>), or host it somewhere "
                    "that serves bytes.")
              sys.exit(1)
          else:
              print(f"::error::Unrecognised file (magic {magic!r}). Expected an XEX2 executable or "
                    "an XContent package (CON/LIVE/PIRS).")
              sys.exit(1)

          os.makedirs('assets/game', exist_ok=True)
          if kind == 'xex':
              os.replace('/tmp/download.bin', 'assets/game/default.xex')
              print("a bare default.xex -> assets/game/default.xex")
          else:
              print("an XContent package -> tools/extract_stfs.py")
              subprocess.run([sys.executable, 'tools/extract_stfs.py', '/tmp/download.bin',
                              '-o', 'assets/game'], check=True)
              if not os.path.exists('assets/game/default.xex'):
                  print("::error::extract_stfs.py ran but produced no default.xex.")
                  sys.exit(1)
          print(f"assets/game/default.xex: {os.path.getsize('assets/game/default.xex')} bytes")
          PY

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: install ninja, unzip and the host compiler
        run: |
          sudo apt-get update
          # clang is installed HERE rather than with the runtime's -dev packages eight steps down,
          # because the step that builds XenonRecomp comes FIRST and pins the compiler into that
          # build tree's CMakeCache.txt. A runner image already ships a clang and apt changes what
          # the bare name resolves to; configuring with one and compiling with the other is not a
          # hypothetical here, it is what this job did, and the failure it produces names a
          # compiler in a step that did not choose one.
          sudo apt-get install -y ninja-build unzip clang

      - name: install Gradle ${{ env.GRADLE_VERSION }}
        run: |
          curl -fsSL --retry 3 -o /tmp/gradle.zip \
            "https://services.gradle.org/distributions/gradle-${GRADLE_VERSION}-bin.zip"
          sudo unzip -q /tmp/gradle.zip -d /opt
          echo "/opt/gradle-${GRADLE_VERSION}/bin" >> "$GITHUB_PATH"

      - name: make sure the pinned NDK is installed
        run: |
          NDKV=$(sed -n 's/^cw\.ndkVersion=\(.*\)$/\1/p' android/gradle.properties)
          SDK="${ANDROID_HOME:-${ANDROID_SDK_ROOT}}"
          [ -d "$SDK/ndk/$NDKV" ] || yes | "$SDK/cmdline-tools/latest/bin/sdkmanager" "ndk;$NDKV" >/dev/null
          # Same as the apk job: install the platform compileSdk names rather than trusting the
          # runner image to have been baked with it.
          yes | "$SDK/cmdline-tools/latest/bin/sdkmanager" \
            "platforms;android-35" "build-tools;35.0.0" >/dev/null
          echo "ANDROID_NDK_HOME=$SDK/ndk/$NDKV" >> "$GITHUB_ENV"

      - name: clone recompilers at pinned bases and apply local patches
        run: |
          git clone --recursive https://github.com/hedge-dev/XenonRecomp ../XenonRecomp
          git -C ../XenonRecomp checkout $XENON_BASE
          git -C ../XenonRecomp submodule update --init --recursive
          # The patch is not optional here in a way it is not optional anywhere else either, but
          # this is the job where skipping it fails QUIETLY: config/CaseWest.toml records that this
          # title's XEX uses the devkit all-zero key, that stock XenonRecomp hardcodes the retail
          # key, and that the wrong key yields an empty Image with no diagnostic at all. The patch
          # is what makes Xex2LoadImage try both.
          git -C ../XenonRecomp apply "$PWD/tools/ci/xenonrecomp-local.patch"
          git clone https://github.com/hedge-dev/XenosRecomp ../XenosRecomp
          git -C ../XenosRecomp checkout $XENOS_BASE
          git -C ../XenosRecomp submodule update --init thirdparty/dxc-bin
          git -C ../XenosRecomp apply "$PWD/tools/ci/xenosrecomp-local.patch"

      - name: build the host XenonRecomp (the tool that generates ppc/)
        run: |
          # ../XenonRecomp/build is configured ONCE, here, with the compiler named. The step that
          # builds the host runtime used to run cmake over this same directory again with clang
          # after this one had configured it with whatever cc the runner defaulted to — two
          # compilers and one CMakeCache.txt, which is either a conflict at re-configure or GCC
          # objects kept and relinked. Everything downstream reads this tree, so this is the only
          # place it is configured, and build.yml's Linux job pins the same two flags for the same
          # directory.
          #
          # The output is teed to a file because a red step here is otherwise UNREADABLE: the
          # job-log API does not serve it, and this job's artifact is assembled from log FILES, so
          # a step that writes none leaves nothing behind. That is precisely what happened the
          # first time this step failed — the run reported "failure" at this step and there was no
          # text anywhere to read.
          # Both through tools/ci/run_logged.sh, which appends to the one xenonrecomp-build.log and
          # turns a failure into an annotation. This is the step whose first failure was reported as
          # "Process completed with exit code 1." and nothing more, because the artifact this job
          # uploads is assembled from log files and this step wrote none.
          tools/ci/run_logged.sh xenonrecomp-build.log \
            cmake -S ../XenonRecomp -B ../XenonRecomp/build -G Ninja \
              -DCMAKE_BUILD_TYPE=Release \
              -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++
          tools/ci/run_logged.sh xenonrecomp-build.log \
            cmake --build ../XenonRecomp/build --target XenonRecomp -j"$(nproc)"
          BIN=../XenonRecomp/build/XenonRecomp/XenonRecomp
          if [ ! -x "$BIN" ]; then
            echo "::error::$BIN is not executable after a build that reported success. Neither the"
            echo "::error::root CMakeLists.txt, nor XenonRecomp's, nor thirdparty's sets"
            echo "::error::CMAKE_RUNTIME_OUTPUT_DIRECTORY, so the target's own binary directory is"
            echo "::error::the expected location. What the tree actually contains:"
            find ../XenonRecomp/build -maxdepth 2 \( -type d -o -name 'XenonRecomp*' \) -print
            exit 1
          fi

      - name: generate the guest image
        run: |
          # `ppc/` must exist first: XenonRecomp segfaults in fwrite otherwise, which is recorded
          # in CLAUDE.md's Commands section as a thing somebody already paid for.
          mkdir -p ppc
          # Run from config/, because CaseWest.toml's paths are relative to it:
          # file_path = "../assets/game/default.xex", out_directory_path = "../ppc",
          # switch_table_file_path = "CaseWest_switch_tables.toml" (committed, so the 205 jump
          # tables do not have to be re-derived here).
          # bash -c because the command has to run from config/ and run_logged.sh takes a command,
          # not a subshell. The LOG path is resolved by the wrapper from THIS directory — the
          # checkout root, where the artifact step looks — and not from the one the tool cd's into.
          tools/ci/run_logged.sh ppc-generate.log bash -c \
            'cd config && ../../XenonRecomp/build/XenonRecomp/XenonRecomp \
               CaseWest.toml ../../XenonRecomp/XenonUtils/ppc_context.h'
          echo "generated $(find ppc -name '*.cpp' | wc -l) .cpp files, $(du -sh ppc | cut -f1)"

      - name: verify the generation was not the silent empty-image failure
        run: |
          # The one check that catches the trap config/CaseWest.toml documents. A wrong decryption
          # key does not error: it produces an Image with no sections, XenonRecomp emits almost
          # nothing, exits 0, and the failure surfaces an hour later as an APK that boots to a
          # crash. Counting the mapping table costs a second and turns that into a named error.
          MAP=ppc/ppc_func_mapping.cpp
          test -f "$MAP" || { echo "::error::$MAP is missing — XenonRecomp produced no image."; exit 1; }
          # An entry is `\t{ 0x82XXXXXX, sub_82XXXXXX },` and every address in this title is
          # inside the image at 0x82000000, so an 8-hex-digit 0x8... is one entry and nothing
          # else in that file matches it.
          N=$(grep -c '0x8[0-9A-Fa-f]\{7\}' "$MAP" || true)
          echo "mapping entries referencing guest addresses: $N (this title is documented at 58,448 functions)"
          # Published as an annotation as well as printed, because the numbers a run produces are the
          # thing worth knowing from it and the step log is not reachable through the public API —
          # only verdicts and annotations are. A count that is only printed is a count nobody can read
          # after the run, which is the same defect as a failure that is only printed.
          echo "::notice::guest image: $N mapping entries reference guest addresses (documented expectation 58,448 functions)"
          if [ "$N" -lt 40000 ]; then
            echo "::error::Only $N entries. That is the signature of an image XenonRecomp could not"
            echo "::error::decrypt — almost certainly the XEX key. config/CaseWest.toml records that"
            echo "::error::this title uses the devkit all-zero key and that tools/ci/xenonrecomp-local.patch"
            echo "::error::is what makes Xex2LoadImage try both. Check that the patch applied above."
            exit 1
          fi

      # ---------------------------------------------------------------------------------------
      # The shader cache, built HERE rather than on the phone.
      #
      # gpu/shader_prebuild.cpp reads the game's own banks — data/shaders/deadrisingepilogue-ps.big
      # holds 1,265 distinct pixel shaders with their runtime microcode verbatim — and translates
      # every one the cache does not hold. It needs DXC, and on THIS runner DXC is the x86_64
      # library XenosRecomp already vendors, which is the cheap half of the problem: SPIR-V is
      # architecture-independent and the cache key is the hash of the MICROCODE rather than of the
      # compiler, so a cache built on x86_64 is exactly what an arm64 device looks up.
      #
      # The host runtime that runs the pass is built against the STUB guest image, and that is not
      # a shortcut that changes the result: --build-shader-cache is a CLI branch that returns
      # before the guest exists, and the pass is pure host code (decode .big -> decode .po/.vo ->
      # HLSL -> SPIR-V). Linking the real 58k-function image here would cost an hour to produce a
      # binary that never calls into it.
      # ---------------------------------------------------------------------------------------
      - name: install the host runtime's dependencies
        run: |
          # Only the runtime's own -dev packages. ninja-build and clang were installed before the
          # XenonRecomp build because that build pins clang into a CMakeCache.txt; installing clang
          # again here would be a no-op at best and would move what the bare name resolves to after
          # the fact at worst.
          sudo apt-get update
          sudo apt-get install -y libsdl2-dev libvulkan-dev libavcodec-dev libavutil-dev

      - name: build the host runtime (stub image — the shader pass never executes guest code)
        run: |
          # No cmake -S ../XenonRecomp here any more. That tree was configured with clang by the
          # step that built the tool; re-configuring it was the second compiler on one cache. The
          # three targets are built rather than assumed, and the build is nearly free because the
          # XenonRecomp executable links all three and so already produced them.
          cmake --build ../XenonRecomp/build --target XenonUtils fmt xxhash -j"$(nproc)"
          python3 tools/gen_stub_ppc.py --out ../stub-ppc --xenon ../XenonRecomp
          # host-runtime-configure.log was listed in this job's artifact paths from the start and
          # was never written by anything — the name was a promise the steps did not keep. It is
          # written here. The old `| tail -5` is gone: five lines of a failing CMake configure is
          # the TAIL of a wall of text whose first line is the error, and run_logged.sh shows the
          # whole thing on the console while putting the last forty in an annotation.
          tools/ci/run_logged.sh host-runtime-configure.log \
            cmake -S runtime -B runtime/build-host -G Ninja \
              -DCW_PPC_DIR="$PWD/../stub-ppc" \
              -DXENON_ROOT="$PWD/../XenonRecomp" \
              -DXENON_BUILD="$PWD/../XenonRecomp/build" \
              -DXENOS_ROOT="$PWD/../XenosRecomp" \
              -DCW_XLIVE=OFF
          tools/ci/run_logged.sh host-runtime-configure.log \
            cmake --build runtime/build-host -j"$(nproc)"
          test -x runtime/build-host/cw_runtime

      - name: build the shader cache from the game's own banks
        run: |
          BANK=assets/game/data/shaders/deadrisingepilogue-ps.big
          if [ ! -f "$BANK" ]; then
            echo "::warning::$BANK is not here, so no shader cache can be built. That means a bare"
            echo "::warning::default.xex was supplied rather than the STFS package: the executable"
            echo "::warning::holds no .big archives, and they are the only source of shader"
            echo "::warning::microcode. The APK still boots; every shader is then translated on the"
            echo "::warning::phone at first sight, which needs the DXC built below."
            exit 0
          fi
          # CW_DXC_LIB rather than relying on the search order: the vendored dxc-bin is a sibling
          # checkout here (../XenosRecomp), not the $HOME/GithubRepo/XenosRecomp the dev-layout
          # candidate spells, and a variable that exists for exactly this beats a symlink.
          CW_ROOT="$PWD" \
          CW_DXC_LIB="$PWD/../XenosRecomp/thirdparty/dxc-bin/lib/x64/libdxcompiler.so" \
            tools/ci/run_logged.sh shader-cache.log \
              runtime/build-host/cw_runtime --build-shader-cache "$BANK" "$PWD/assets/shader_spv"
          N=$(find assets/shader_spv -name '*.spv' 2>/dev/null | wc -l)
          echo "shader cache: $N module(s)"
          # The banks are documented at 1,265 distinct pixel shaders. Zero means the pass did not run
          # at all — a wrong bank, or DXC missing — and is worth failing on rather than shipping an
          # APK that quietly translates everything on a phone CPU instead.
          if [ "$N" -eq 0 ]; then
            echo "::error::the shader pass produced nothing. In shader-cache.log look for the DXC"
            echo "::error::load line ([shxlate] dxcompiler: ...) and for the bank's own header."
            exit 1
          fi
          # The vertex half runs only because tools/release/vs_recipes.bin is TRACKED IN THIS
          # REPOSITORY (107 recipes, magic ZCVR, generated by tools/vs_recipes.py from a census of
          # a machine that ran the game). CW_ROOT above is what lets HostPaths::VsRecipes() find
          # it: the pass resolves recipes relative to the data root, and without that variable it
          # would quietly do the pixel half only — a pass that reports success and is missing 104
          # shaders is worse than one that fails.
          if [ -f tools/release/vs_recipes.bin ]; then
            echo "vs_recipes.bin present — the VERTEX half ran too, so the cache should hold"
            echo "102 of the 104 runtime vertex shaders. The two that remain are engine-"
            echo "synthesised with no template anywhere (vs_539ea9e08aa83f0c, vs_a4ae7c2b7c1818c4)"
            echo "and are bound at boot before any visible frame: they are why the arm64 DXC"
            echo "below is required rather than an optimisation."
          else
            echo "::warning::no tools/release/vs_recipes.bin — the pass was PIXEL-ONLY and all 104"
            echo "::warning::vertex shaders will be translated on the phone at first sight."
          fi

      - name: cache the cross-compiled dependencies
        uses: actions/cache@v4
        with:
          path: |
            third_party/android/${{ inputs.abi }}/sdl2
            third_party/android/${{ inputs.abi }}/ffmpeg
            third_party/android/${{ inputs.abi }}/adrenotools
            third_party/android/${{ inputs.abi }}/dxc
            third_party/android/sdl-java
            ../XenonRecomp/build-android-${{ inputs.abi }}
          key: cw-android-deps-${{ inputs.abi }}-${{ hashFiles('tools/android/**', 'android/gradle.properties') }}
          restore-keys: cw-android-deps-${{ inputs.abi }}-

      - name: build the APK from the real image
        run: |
          # --with-dxc is what makes this artifact playable rather than merely real: with no
          # arm64 libdxcompiler.so the phone cannot translate a shader, and the vertex half is
          # never in the cache above (no vs_recipes.bin), so it would be refused at the draw
          # that first binds it. CW_SHADER_SPV stages the cache into the APK's assets and the
          # launcher copies it to <root>/assets/shader_spv/ at first run.
          CW_PPC_DIR="$PWD/ppc" \
          CW_SHADER_SPV="$([ -d assets/shader_spv ] && echo "$PWD/assets/shader_spv")" \
            tools/ci/run_logged.sh playable-build.log \
              tools/android/build_apk.sh --abi "${{ inputs.abi }}" --with-dxc

      - name: publish the logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-playable-logs
          if-no-files-found: ignore
          path: |
            xenonrecomp-build.log
            ppc-generate.log
            host-runtime-configure.log
            shader-cache.log
            playable-build.log

      - name: publish the APK, if it was asked for
        if: inputs.artifact != 'logs only'
        uses: actions/upload-artifact@v4
        with:
          name: casewest-android-playable-apk
          path: android/app/build/outputs/apk/**/*.apk
          if-no-files-found: error
          # One day, and the choice input that got here said so in its own label. This APK contains
          # code derived from a copyrighted executable and the repository is public, which makes
          # the artifact a public download for as long as it is retained. Nothing in a workflow can
          # make that private; the only lever is how long the window is, so it is as short as
          # GitHub allows while still being usable.
          retention-days: 1

      - name: what this artifact is, and what it still needs
        if: always()
        run: |
          SPV=$(find assets/shader_spv -name '*.spv' 2>/dev/null | wc -l)
          DXC=$(find third_party/android -name libdxcompiler.so 2>/dev/null | head -1)
          echo "==================== READ THIS ===================="
          echo "guest image : REAL (generated from your XEX in this run)"
          echo "abi         : ${{ inputs.abi }}"
          echo "published   : ${{ inputs.artifact }}"
          echo "shader cache: ${SPV} module(s) built on this runner from the game's own banks"
          echo "              (the documented expectation for the pixel half is 1,265)"
          echo "device DXC  : ${DXC:-NONE — shaders cannot be translated on the phone}"
          echo
          if [ "${SPV:-0}" -eq 0 ]; then
            echo "THE CACHE IS EMPTY, which means the banks were not here: a bare default.xex"
            echo "was supplied instead of the STFS package. Every shader is then a first-sight"
            echo "translation on the phone, which works only if the device DXC line above is not"
            echo "NONE, and is slow the first time it happens."
          else
            # One annotation carrying what this run actually produced, for the same reason as the
          # guest-image count above: verdicts and annotations are served by the public API, step logs
          # and artifacts are not. This step runs `if: always()`, so the summary exists on a failed
          # run too — which is when knowing how far it got matters most.
          APK=$(find android/app/build/outputs/apk -name '*.apk' 2>/dev/null | head -1 || true)
          APKDESC="none — the build did not get as far as packaging"
          [ -n "$APK" ] && APKDESC="$(stat -c%s "$APK") bytes"
          DXCDESC="ABSENT, so shaders cannot be translated on the phone"
          [ -n "$DXC" ] && DXCDESC="present"
          echo "::notice title=what this run produced::abi=${{ inputs.abi }}; shader cache=${SPV} module(s), where the pixel half is documented at 1,265 and the vertex half at 102 of 104; device DXC ${DXCDESC}; APK ${APKDESC}"
          echo "That cache travels inside the APK. tools/release/vs_recipes.bin is tracked in"
            echo "this repository, so the pass covered the VERTEX half too: 102 of the 104"
            echo "runtime vertex shaders are template+patch, recovered from the disc bank."
            echo "Expect roughly 1,265 pixel modules plus 102 vertex ones in the count above."
          fi
          echo
          echo "The two vertex shaders that remain have no disc template at all — they are"
          echo "engine-synthesised, and they are the 2nd and 4th shaders the title binds, at boot,"
          echo "before any visible frame. A residue of two is not a small gap when both are bound"
          echo "before the first frame: without them nothing draws. That is why the device DXC line"
          echo "above decides whether this artifact works, and why it is built from source — there"
          echo "is no arm64 libdxcompiler.so to download, from Microsoft or from the dxc-bin mirror"
          echo "XenosRecomp vendors."
          echo "==================================================="
